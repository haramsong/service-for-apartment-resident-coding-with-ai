# globals.css CSS 변수 중복 선언 해결

*작성일: 2025-11-03*

*작성자: Designer Agent*

*이슈 유형: CSS 설정 오류*

*우선순위: High*

## 📋 문제 상황

### 발견된 문제점
- `--background` 변수가 두 번 선언됨
- shadcn/ui 형식(`0 0% 100%`)과 hex 형식(`#ffffff`)이 혼재
- `--border` 변수도 중복 선언
- CSS 변수 참조 시 `hsl()` 함수 누락

## 🔍 원인 분석

### 1. 변수 중복 선언
```css
/* shadcn/ui 표준 */
--background: 0 0% 100%;

/* 커스텀 변수 (중복) */
--background: #ffffff;
```

### 2. 형식 불일치
- shadcn/ui는 HSL 색상 공간 사용 (예: `0 0% 100%`)
- 커스텀 변수는 hex 형식 사용 (예: `#ffffff`)
- 참조 시 `hsl()` 함수 필요

### 3. 네이밍 충돌
- shadcn/ui 예약 변수명과 커스텀 변수명 충돌
- 명확한 구분 필요

## ✅ 해결 방법

### 1. 커스텀 변수명 변경
```css
/* 변경 전 */
--background: #ffffff;
--background-secondary: #f8f9fa;
--background-muted: #f3f4f6;
--border: #e5e7eb;

/* 변경 후 */
--bg-primary: #ffffff;
--bg-secondary: #f8f9fa;
--bg-muted: #f3f4f6;
--border-color: #e5e7eb;
```

### 2. shadcn/ui 변수 참조 시 hsl() 함수 사용
```css
/* 올바른 사용법 */
background-color: hsl(var(--background));
border-color: hsl(var(--border));
```

### 3. 다크모드 변수명 통일
```css
.dark {
  --bg-secondary: #1e293b;
  --bg-muted: #334155;
  --border-color: #334155;
}
```

## 📊 개선 효과

### 1. 변수 충돌 해결
- shadcn/ui 컴포넌트 정상 작동
- 커스텀 스타일과 명확히 구분

### 2. 일관성 확보
- 모든 shadcn/ui 변수는 `hsl()` 함수로 참조
- 커스텀 변수는 직접 참조

### 3. 유지보수성 향상
- 명확한 네이밍으로 변수 용도 파악 용이
- 다크모드 지원 개선

## 🔄 예방 방법

### 1. 네이밍 규칙 수립
- **shadcn/ui 예약 변수**: 그대로 사용, `hsl()` 함수로 참조
- **커스텀 변수**: 명확한 접두사 사용 (`--bg-`, `--border-color` 등)

### 2. CSS 변수 가이드라인
```css
/* shadcn/ui 변수 사용 */
background-color: hsl(var(--background));
color: hsl(var(--foreground));

/* 커스텀 변수 사용 */
background-color: var(--bg-primary);
border-color: var(--border-color);
```

### 3. 정기 검증
- CSS 변수 중복 선언 체크
- shadcn/ui 컴포넌트 렌더링 테스트
- 다크모드 전환 테스트

## 📚 참고 문서
- [shadcn/ui Theming](https://ui.shadcn.com/docs/theming)
- [아파트 커뮤니티 색상 시스템](../apartment-community-color-system.md)

---

*CSS 변수 중복이 해결되어 shadcn/ui 컴포넌트가 정상 작동합니다.*
